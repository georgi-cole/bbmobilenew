name: Temporary Confessional formatter

on:
  push:
    branches: [agent/confessional-flow-v2]

permissions:
  contents: write

jobs:
  format-confessional:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/confessional-flow-v2
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Format Confessional files
        run: >-
          npx prettier --write
          src/routes.tsx
          src/routes/ConfessionalFlowBridge.tsx
          src/screens/DiaryRoom/ConfessionalRoute.tsx
          src/screens/DiaryRoom/RequiredConfessionalDecision.tsx
          src/screens/DiaryRoom/RequiredConfessionalSession.tsx
          src/screens/DiaryRoom/RequiredConfessionalSession.css
          src/screens/DiaryRoom/requiredConfessionalPresentation.ts
      - name: Commit formatting
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add src/routes.tsx src/routes/ConfessionalFlowBridge.tsx src/screens/DiaryRoom
          git commit -m "Format required Confessional flow"
          git push origin HEAD:agent/confessional-flow-v2
